export function chinaClock(now=new Date()){const parts=new Intl.DateTimeFormat('sv-SE',{timeZone:'Asia/Shanghai',year:'numeric',month:'2-digit',day:'2-digit',hour:'2-digit',minute:'2-digit',hourCycle:'h23'}).formatToParts(now);const p=Object.fromEntries(parts.map(x=>[x.type,x.value]));return {date:`${p.year}-${p.month}-${p.day}`,time:`${p.hour}:${p.minute}`}}
export function due(record,clock){const minute=s=>Number(s.slice(0,2))*60+Number(s.slice(3));const delay=minute(clock.time)-minute(record.settings.time);return !record.settings.paused&&record.sentDate!==clock.date&&delay>=0&&delay<=15&&record.retryAt<=Date.now()}
